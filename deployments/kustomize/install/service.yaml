kind: Service
apiVersion: v1
metadata:
  name: xmarecek-ambulance-webapi
spec:
  selector:
    pod: xmarecek-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port